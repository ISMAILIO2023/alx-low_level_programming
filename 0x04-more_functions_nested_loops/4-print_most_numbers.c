#include "main.h"

/**
 * print_most_numbers - prints the number from 0 to 9 followed by new line
 * @i : input
 * Return: number
 **/

void print_most_numbers(void)
{
	int i;
	if (i != 2 && i != 4)
	{
	for (i = 0; i < 10; i++)
	  _putchar ( i + '0');
	_putchar('\n');
	}
	return (0);
}

